/* nao deveria funcionar  */


while (naoTerminou || TRUE)
	while (naoTerminou || TRUE)
		begin
			/* 1 comando  */
			writeln("Fim");
			/* 2 comando  */
			if ( ( n / m ) !=  n * n )
			writeln("Fim"); /* nao deveria funcionar aq*/
				if((m = 2*o) || (  n >= m/n))
					begin
						if ( ( n / m ) !=  n * n )  writeln("Sim"); else writeln("nao");
						writeln("Fim");
					endif
				else
					begin
						if((m = 2*o) || (  n >= m/n))
						writeln("Fim");
						readln(n);
					endelse
			/* 3 comando  */
			writeln("Ola' ",nome);
			/* 4 comando  */
			n<-n+1;
			/* 5 comando  */
			naoTerminou<-n<MAXITER;
        endwhile

		

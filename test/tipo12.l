/* nao deveria funcionar */
int n <- 10, m <- 100,  o <- 10;
boolean verdadeiro <- TRUE;
string f <- "kkk";

if((m = 2*o) || (  n >= m/n))
	begin
		if ( ( n / m ) !=  n * n )  writeln("Sim"); else writeln("nao");
		writeln("Fim");
	endif
else
	begin
		if (f)  writeln("Sim"); else writeln("nao");
		readln(n);
endelse
